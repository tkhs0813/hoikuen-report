import { describe, expect, it } from "vitest";
import { mount } from "@vue/test-utils";
import MonthlyReports from "./MonthlyReportList.page.vue";

describe("MonthlyReportList", () => {
  it("is defined", () => {
    // const wrapper = mount(MonthlyReports);
    expect("a").toBe("a");
  });
});
